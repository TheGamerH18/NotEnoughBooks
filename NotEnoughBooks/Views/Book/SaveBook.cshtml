@model bool
@{
    ViewData["Title"] = Model ? "Book Saved" : "Book Not Saved";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<p class="mb-2">Book saved</p>
<a class="btn btn-primary w-100 mb-2" asp-action="Index">Go to list</a>
<a class="btn btn-secondary w-100" asp-action="AddBook">Add another Book</a>